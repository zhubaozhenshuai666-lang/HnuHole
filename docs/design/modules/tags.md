# 通道与自定义标签

更新：2026-09-19。首版范围已确认；当前规则只在本模块维护。

- 状态：通道必选、用户自定义 `#标签` 选填；不做统一中间分类和标签治理；主题屏蔽留到后续版本设计。业务决定见[第二十二轮答复](../../discussions/2026-09-19-grilling-round22.md)。

## 范围

帖子通道归属、自定义标签、添加和筛选。导航见[入口与导航](navigation.md)，普通违规内容处理见[内容管理](moderation.md)。

## 已确认规则

### CATEGORY-01：首版七个通道

- 首版通道为：吐槽、避雷、安利、互助、搭子、科技、情感。
- 原名称“找搭子”已简化为“搭子”，“科技前沿”已简化为“科技”；后续口语提及“找搭子”不重新更名。
- 失物招领等细分内容通过适合的通道和自定义标签表达，不新增同名通道。

### CATEGORY-02：一帖一个通道

- 每帖必须归属一个通道，不同时投放多个通道；从通道内发帖默认归入当前通道。
- 已发布帖的大通道不能更换，按[内容管理 EDIT-POST-01](moderation.md)执行。
- 例如找篮球搭子归入“搭子”，可自定义添加 `#篮球`；安利食堂可添加 `#食堂`。各通道的具体标签由作者和实际内容形成。

### TAG-DIRECTION-02：通道必选，自定义标签选填，不设统一分类

- 首版保留用户自定义 `#标签`；作者可以完全不加标签，不影响所属通道和正常发布。
- 不增加“通道 → 中间分类 → 细标签”的平台分类层级，不维护全站或逐通道的固定主题词表。
- 不做审核员纠正主题、移除无关标签、锁定标签的专项治理，也不做标签新增审批、合并和停用管理。
- 不做标签专项治理不取消普通违规内容举报、处罚或违规昵称头像处置。
- `#tag` 表示用户自定义主题词，不因此引入话题广场、热搜、关注或推荐算法。具体输入方式随 UI 设计确定。

### TAG-01 / TAG-02：标签范围与数量

- 每帖最多三个自定义标签，允许不选；标签不逐一成为导航树节点，也不是切换大通道的入口。
- 同一个词可在不同通道使用，不要求统一标签清单；当前通道的标签筛选仅返回当前通道内容，不混入其他通道帖子。
- 全局文字搜索仍按[SEARCH-01](navigation.md)匹配标题、正文和标签并按通道分组；自定义标签不改变搜索范围。
- 仍有编辑权限的作者可修改帖子标签，保留原帖及已有互动，不重新发布、不更换通道，见[EDIT-POST-01](moderation.md)。

### TAG-03：自定义与已有标签复用

- 作者可输入自定义标签，也可搜索和复用已有标签；常用词或相近词提示仅作为输入辅助，不构成平台固定词表。
- 不做人工同义归并，不根据相近词自动增加标签、分类或屏蔽范围。
- 不根据正文关键词强制归类。例如“骑车去健身”不等于整篇帖子的主题是骑行；首版不增加模型分类功能。

### TAG-SELECT-01：添加标签

- 发布确认页保留选填标签区域；此前已定的底部面板可用于搜索、选择或创建标签，面板保留已选内容，最多三个。
- 点击“完成”回到发布确认页并保留所选标签，不提交帖子。
- 与用户 `#tag` 输入方向的具体交互衔接留给 UI 设计，不把“像抖音一样”扩展为已确认的正文解析或整个外部产品交互。

### TAG-FILTER-01：多标签同时匹配

- 筛选多个标签时，仅显示当前通道内同时包含所有所选标签的帖子。
- 清空标签筛选后恢复当前通道可访问帖子，不跳转到其他通道。
- 筛选不绕过登录、删除、下架及账号权限；首版不叠加主题偏好屏蔽。

### TAG-FILTER-02：通道列表筛选

- 列表上方保留“筛选”入口，打开底部标签面板；筛选最多选择三个标签，点击“完成”后统一应用。
- 已选标签显示在列表上方，可通过每项叉号取消；多标签按 TAG-FILTER-01 同时匹配。
- 具体样式、面板关闭和输入反馈随 UI 设计确定。

### TAG-BLOCK-DEFERRED-01：主题屏蔽留到后续版本

- 用户确认“可以，屏蔽后续版本可以设计”：首版不提供中间分类、标签或主题偏好的屏蔽功能，不增加对应设置入口。
- 通道列表、搜索、热榜、帖子、收藏、分享及私信来源帖均不执行旧中间分类屏蔽；不显示“所属分类已被屏蔽”拦截状态。
- 互动提醒不因旧分类屏蔽而隐藏标题或摘要。系统推送默认隐藏内容、账号访问、删除和下架等独立规则继续有效。
- 私信拉黑及其黑名单、普通举报和处罚保留，不受主题屏蔽延期影响。
- 后续版本重新设计屏蔽对象及作用范围，不预先继承旧中间分类、细标签或关键词方案。

## 后续 UI 与实现事项

- 自定义标签的输入、已有词提示、发布确认和列表筛选画面；沿用已定数量与同时匹配规则，不再重复业务提问。
- 标签字符和长度校验、同名词存储与检索实现；不新增固定分类表或同义词治理。
- 主题屏蔽已明确不在首版，后续版本单独设计。

## 替代与来源

第二十二轮最终答复取代 CATEGORY-BLOCK-01、CATEGORY-EDIT-01 的中间分类方案，以及 TAG-04 / TAG-05、TAG-OWN-01、TAG-MESSAGE-01、TAG-DM-01 的主题屏蔽及其例外。旧规则不再是首版交付要求，不据旧问题继续追问。

历史脉络见[初期讨论](../../discussions/2026-09-06-hainanu-treehole.md)、[第四轮](../../discussions/2026-09-15-grilling-round4.md)与[第二十二轮](../../discussions/2026-09-19-grilling-round22.md)。当前没有标签专项定稿图；发帖画面由[新建帖子](post-composer.md)维护。
