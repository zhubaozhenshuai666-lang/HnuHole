# 第十三轮设计五问：剩余产品边界（已回答）

状态：2026-09-15，用户五项均回答“可以”，全部采纳并写入对应模块。标签专项继续延后。

❓ **Q1 — 处理中发帖卡片显示多久？**（已确认）

➡️ 建议：一直保留到服务端确认成功或失败，不因退出 App 消失。

❓ **Q2 — 放弃旧帖修改后，是否提示？**（已确认）

➡️ 建议：退出直接丢弃，不增加确认提示；原帖保持不变。

❓ **Q3 — 常用标点具体范围如何处理？**（已确认）

➡️ 建议：首版按 Unicode 标点类别放行，控制字符、表情和不可见字符拒绝。

❓ **Q4 — “评论已经删除”提示停留多久？**（已确认）

➡️ 建议：顶部短提示自动消失，不阻塞页面。

❓ **Q5 — 服务端稳定序号异常时怎么办？**（已确认）

➡️ 建议：客户端只按服务端返回的序号排序；缺失时显示加载失败并重试，不自行猜顺序。
