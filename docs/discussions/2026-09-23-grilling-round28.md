# 第二十八轮：任务 1 实现计划确认（已回答）

状态：2026-09-23。用户确认按本计划直接进入开发；本轮不再新增产品范围，只记录执行顺序和完成标准。测试文件和测试运行按用户最新要求留到有 Go/Flutter/Docker 环境的后续轮次。

## 拟执行顺序

1. 建立 `packages/openapi` 的通道目录契约和生成检查。
2. 创建 PostgreSQL 迁移与七通道种子，加入唯一约束和重复执行测试。
3. 在 Go 模块化单体中实现认证保护的 `GET /api/v1/channels`，覆盖 `200/401/503`。
4. 建立开发/测试夹具：账号 A 的 A1/A2、账号 B 的 B1；夹具不进入生产迁移。
5. 在 Flutter 中实现未登录无节点树外壳、登录后完整加载、加载/失败/重试和二维可拖动树。
6. 编写迁移、API、客户端加载和交互四层测试，运行格式化、静态检查和集成测试。

## 完成标准

- 未登录看不到通道目录和节点；邮箱验证成功后完整目录校验通过才显示节点。
- 七个通道只由服务端目录提供，前五个冷启动露出，后两个可拖动探索。
- API、迁移、OpenAPI 生成和四层测试通过；服务端重启不丢通道数据。
- 任务 1 不引入热度、搜索、标签筛选、帖子发布或最终视觉定稿。
