# 第二十五轮：入口树与首条切片验收（已回答）

状态：2026-09-23。用户补充确认未登录入口树与登录后通道节点的边界，并接受任务 1/2 的开发验收建议。

## 用户答复

1. 未登录刚进入时有一棵没有节点的树；邮箱验证成功后，在同一入口上下文中显示完整可拖动的通道节点。
2. 通道使用不可变 `code`、固定 UUID 和版本化迁移，服务端目录为唯一来源。
3. 接受并允许稍微复杂的最小端到端场景，覆盖多身份、跨通道、权限拒绝、服务端重启和草稿恢复。
4. 按进程级重启验收：保留 PostgreSQL Docker volume 和移动端 Drift/SQLite 文件；不要求卸载重装或跨设备恢复。

## 已确认验收边界

- 未登录只渲染树入口外壳，不渲染通道节点、帖子或社区内容；未登录只点击树洞进入邮箱登录。登录成功后请求完整七通道目录，初始露出五个节点，其余节点可探索到达。
- 目录加载中保留无节点树；目录失败不显示部分节点或过期缓存，提供重试；会话失效时清除节点并回到登录入口。
- 任务 1 仍包含真实邮箱验证流程；Mailpit 与开发验证适配器只用于开发测试取码，不绕过会话和权限边界。
- 通道种子数据使用不可变机器标识与固定 UUID，显示名称单独存储；迁移重复执行不得产生重复通道，未来名称变化通过新迁移处理。
- 任务 2 的端到端验收覆盖：账号 A 的两个身份、账号 B 的一个身份；跨通道发帖；同帖换身份被服务端拒绝；主评论与楼中楼回复；服务端进程重启后内容仍可读；移动端进程重启后草稿仍可恢复；“我的”与公开视图遵守身份隔离。

## 下一轮

需要具体确定入口树未登录状态的交互、邮箱验证成功后的节点加载/失败行为，以及任务 1 的 API 契约字段和测试夹具格式。
