# 第二十四轮：导航树开发拆分（已回答）

状态：2026-09-23。用户确认导航树直接进入首条开发切片，并接受推荐的任务拆分、通道来源和首条范围。

## 用户答复

1. 两次任务按推荐拆分。
2. 对导航树“要做就做好的”要求进一步澄清后，接受高质量二维功能树方案。
3. 通道目录由服务端数据库和 API 唯一提供。
4. 首条切片暂不接热度、搜索和标签筛选。

## 已确认任务边界

- **任务 1：通道目录与基础导航树。** 服务端用种子数据建立七个固定通道并提供稳定 ID；客户端实现登录后的高质量二维功能树。默认露出吐槽、避雷、安利、搭子、情感，互助和科技通过拖动探索进入。拖动、层次变化、点击反馈、屏幕适配和返回位置恢复纳入验收；真实三维和最终视觉定稿不纳入。
- **任务 2：导航树接入文字内容主链。** 从节点进入通道列表，接入文字帖子、详情、主评论、楼中楼回复和“我的”帖子/草稿，并验证身份固定、身份隔离和持久化。
- 通道目录的业务唯一来源是服务端数据库；客户端不维护另一份可变清单。通道增删改管理不在这两次任务内。
- 首条切片不实现动态通道热度、通道内或全局文字搜索、标签输入/复用/筛选；这些功能不改变既定通道归属规则。

## 下一轮

需要确定任务 1 的登录与身份前置、通道种子数据的版本/幂等行为，以及任务 2 的最小端到端验收场景。
