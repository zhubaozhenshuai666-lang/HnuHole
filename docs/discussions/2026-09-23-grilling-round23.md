# 第二十三轮：首条开发切片边界（已回答）

状态：2026-09-23。用户确认首条开发切片的通道入口、标签范围和持久化验收；导航树直接进入开发，具体任务拆分在下一轮确定。

## 用户答复

1. 默认露出的五个通道中，将“互助”改为“情感”。
2. 直接实现导航树，可以拆成两次任务。
3. 按建议执行：保留可选 `#标签` 字段，首条切片暂不实现标签输入、复用和筛选。
4. 按建议执行：服务端内容与身份写入 PostgreSQL，本机草稿写入 Drift/SQLite，重启后可恢复，不要求跨设备草稿同步。

## 已确认范围

- 首版七个通道仍为：吐槽、避雷、安利、互助、搭子、科技、情感。
- 无热度数据时导航树初始露出：吐槽、避雷、安利、搭子、情感；“互助”和“科技”仍可通过导航树探索进入。
- 首条切片包含基础导航树，拆为两次开发任务；节点视觉和动效细节留到 UI 阶段。
- 首条切片仍验证登录、身份初始化、进入通道、文字帖子、帖子详情、主评论、楼中楼回复和“我的”帖子/草稿，并验证帖内身份固定与不同身份隔离。
- 首条切片的持久化验收覆盖服务端已发布帖子、评论、身份和通道归属，以及本机草稿；不包含跨设备草稿同步。

## 下一轮

需要继续明确两次导航树开发任务各自的交付边界、第一条任务的验收场景，以及导航树与通道列表之间的最小接口。
