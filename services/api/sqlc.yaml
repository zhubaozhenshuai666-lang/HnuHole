version: "2"

sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: "internal/channels/queries.sql"
    gen:
      go:
        package: "channelsdb"
        out: "internal/channels/channelsdb"
        sql_package: "pgx/v5"
        emit_json_tags: true
